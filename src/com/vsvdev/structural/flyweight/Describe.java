package com.vsvdev.structural.flyweight;

/**
 *
 * Типовая задача.
 * Есть группа тяжелых объектов. У всех объектов имеется одинаковая
 * часть, причем это самая значительная часть объекта. Эту одинаковую
 * часть можно вынести из всех объектов, а в самих объектах оставить
 * только уникальные свойства.
 * Важно помнить, что общая часть не должна меняться. А если и
 * меняться, то у всех объектов сразу.
 * Часто таким большим общим объектом является кэш.
 */
public class Describe {
}
